{
  "name": "options-detective-backend",
  "version": "1.8.0",
  "private": true,
  "description": "Express API + Python math engine bridge for Options Detective",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "build:math": "math\\.venv\\Scripts\\pyinstaller.exe --noconfirm --clean --onedir --console --name od-math --distpath math/dist --workpath math/build --specpath math math/od_math.py"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
