{
  "name": "options-detective-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express API + Python math engine bridge for Options Detective",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
